{
    "version": 2,
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index.js"
        },
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ],
    "installCommand": "npm install && cd api && npm install --omit=dev && cd ..",
    "buildCommand": "cd FrontEnd && npm install && npm run build",
    "outputDirectory": "FrontEnd/dist"
}